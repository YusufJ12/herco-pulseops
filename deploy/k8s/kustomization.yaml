apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: pulseops

resources:
  - namespace.yaml
  - pvc.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - networkpolicy.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/part-of: pulseops-monitoring
